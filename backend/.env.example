# Copy this file to .env and adjust if needed.
# These defaults match docker-compose.yml exactly, so if you used
# `docker compose up -d` with no changes, you don't need to edit anything.

DATABASE_URL=postgresql+psycopg2://qode:qode_password@localhost:5432/qode_backtester
FRONTEND_ORIGIN=http://localhost:5173
