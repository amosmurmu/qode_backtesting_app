# Copy this to .env if you need to point the frontend at a different backend URL.
# Default (no .env needed) assumes the backend is running on localhost:8000.

VITE_API_BASE_URL=http://localhost:8000/api
